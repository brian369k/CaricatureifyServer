fastapi==0.110.0
uvicorn==0.30.1

# PyTorch + vision matched pair (Python 3.13 + CUDA 12.1 wheels)
torch==2.7.0
torchvision==0.22.0

# Core deps
numpy==1.26.4
pillow==10.4.0

# Diffusion + transformer stack
diffusers==0.27.2
transformers==4.37.2
tokenizers==0.15.2
safetensors==0.4.2
accelerate==0.29.3
pyyaml==6.0.1
python-multipart==0.0.9

# Pull from PyTorch wheel index (prebuilt binaries — NO compile hell)
--extra-index-url https://download.pytorch.org/whl/cu121
